<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_split_pane"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="horizontal">

    <!-- each frame layout will have weight set programmatically,
        however we want to ensure that they have some size on first pass, so set width to 10dp -->

    <FrameLayout
        android:id="@+id/content_split_left_pane"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="@color/white" />

    <RelativeLayout
        android:id="@+id/split_pane_divider"
        android:layout_width="10dp"
        android:layout_height="match_parent"
        android:background="@color/simple_grey">

        <ImageView
            android:layout_width="15dp"
            android:layout_height="15dp"
            android:layout_centerInParent="true"
            android:background="@drawable/ic_divider_pane" />

    </RelativeLayout>

    <FrameLayout
        android:id="@+id/content_split_right_pane"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:background="@color/white" />

</LinearLayout>